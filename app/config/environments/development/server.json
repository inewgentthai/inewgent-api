{
  "host": "${process.env.HOST || 'localhost'}",
  "port": "${process.env.PORT || 1337}",
  "proxy": {
    "enabled": true,
    "host": "api.inewgent.local",
    "port": 443,
    "ssl": true
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
